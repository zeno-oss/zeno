{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "$server": ["../../packages/server/index.ts"],
      "$store": ["utils/store.ts"],
      "$trpc": ["utils/trpc.ts"]
    }
  }
}
