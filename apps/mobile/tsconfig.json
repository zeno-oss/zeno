{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "paths": {
      "$server": ["../../packages/server/index.ts"],
      "$store": ["../../packages/store/index.ts"],
      "$trpc": ["utils/trpc.ts"]
    }
  }
}
